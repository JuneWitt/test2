<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/purple2</item>
        <item name="colorPrimaryDark">@color/black_semi_transparent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

        <style name="menu_labels_style">
            <item name="android:background">@drawable/fab_label_background</item>
            <item name="android:textColor">@color/white</item>
        </style>

    <style name="Fullscreen" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="SplashTheme" parent="Fullscreen">
        <item name="android:windowBackground">@drawable/side_nav_bar</item> <!--splash pic-->
    </style>

</resources>
